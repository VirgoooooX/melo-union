name: provider_qq
description: QQ Music provider adapter for MeloUnion.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  http: ^1.6.0
  provider_contract:
    path: ../provider_contract

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
