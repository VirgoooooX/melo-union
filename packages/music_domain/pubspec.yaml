name: music_domain
description: Provider-agnostic MVP domain logic for MeloUnion.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  provider_contract:
    path: ../provider_contract

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
