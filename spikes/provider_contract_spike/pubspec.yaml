name: provider_contract_spike
description: Provider registry and capability spike for MeloUnion Phase 0.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
